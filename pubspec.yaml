name: db_commons
description: data base to handle app memory
publish_to: none

environment:
  sdk: ">=2.18.4 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.15
 
dev_dependencies:
  mockito: ^5.4.0
  build_runner: ^2.3.3
  test:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  very_good_analysis: ^3.1.0
